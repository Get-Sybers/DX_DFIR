---
# pinned — never :latest, never a bare branch (ansible-standards infra galaxy §2)
collections:
  - name: community.docker
    version: "3.10.3"
  # ansible.cfg enables its profile_tasks callback — a runtime dependency
  - name: ansible.posix
    version: "2.1.0"
  # dxdfir_stack's host-side TLS generation
  - name: community.crypto
    version: "2.26.9"
  # NOT here: the build galaxy — its pin is the GITLINK, resolved in place via
  # roles_path (docs/reference/repository-map.md); its dep range is satisfied by the pins above
