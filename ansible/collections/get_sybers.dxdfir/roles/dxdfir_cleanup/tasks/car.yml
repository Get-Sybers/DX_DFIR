---
- name: "cleanup-car | select the CAR tree"
  ansible.builtin.set_fact:
    dxdfir_cleanup_wipe_dir: "{{ dxdfir_cleanup_car_dir }}"
  tags: [always]

- name: "cleanup-car | wipe the tree (preserving .gitkeep)"
  ansible.builtin.include_tasks: wipe.yml
