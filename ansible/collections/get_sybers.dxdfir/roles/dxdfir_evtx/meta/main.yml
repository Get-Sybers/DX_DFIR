---
galaxy_info:
  role_name: dxdfir_evtx
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Parse Windows Event Logs (.evtx) with goevtx into JSON Lines. Runs the
    self-orchestrating get-sybers/goevtx image from its contract; one folder of
    goevtx.jsonl per log.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, evtx, windows, host]

dependencies: []
