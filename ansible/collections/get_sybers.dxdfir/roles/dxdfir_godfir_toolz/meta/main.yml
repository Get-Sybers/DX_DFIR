---
galaxy_info:
  role_name: dxdfir_godfir_toolz
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process disk images with the GoDFIR-toolz parsers (gore, gojle, gole,
    goamcache, goappcompat, gosbe, gorb, gomft) plus a plaso-driven
    SRUM parse into per-host artefact output.
    Invokes the get_sybers_dxdfir.godfir_toolz Python processor; one output dir per host.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, godfir, registry, amcache, srum, forensics]

dependencies: []
