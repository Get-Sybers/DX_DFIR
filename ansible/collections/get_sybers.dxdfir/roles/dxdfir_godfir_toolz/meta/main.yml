---
galaxy_info:
  role_name: dxdfir_godfir_toolz
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process disk images with the GoDFIR-toolz parsers (gore, gojle, gole,
    goamcache, goappcompat, gosbe, gorb, gomft, goese, goprefetch): the plaso
    image exports the artefact set, each Go tool image runs over it from its
    contract; one folder per tool and item.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, godfir, registry, amcache, srum, forensics]

dependencies: []
