---
galaxy_info:
  role_name: dxdfir_plaso
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process forensic disk images, VM exports and loose trees with Plaso
    (log2timeline + psort) into JSON Lines. Drives the multi-tool
    get-sybers/plaso image from its contract; one timeline.jsonl per source plus
    the durable .plaso storage file.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, plaso, log2timeline, disk, timeline]

dependencies: []
