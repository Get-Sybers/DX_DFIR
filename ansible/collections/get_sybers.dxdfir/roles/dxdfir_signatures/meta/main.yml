---
galaxy_info:
  role_name: dxdfir_signatures
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Run the YARA, Suricata, Hayabusa and disk-scan detection sub-tools of the
    get-sybers/signatures image over the evidence, from its contract; one
    <subtool>/ folder of detection JSON Lines.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, yara, suricata, hayabusa, detection]

dependencies: []
