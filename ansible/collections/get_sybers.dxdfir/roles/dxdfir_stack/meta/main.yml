---
galaxy_info:
  role_name: dxdfir_stack
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Lifecycle for the docker-compose Elastic-native analysis backend:
    deploy/destroy/start/stop/status. Elastic uses prebuilt images and
    docker/elastic/.env.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, stack, elastic, docker]

dependencies: []
