---
galaxy_info:
  role_name: dxdfir_stack
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Lifecycle for the docker-compose Elastic analysis backend:
    deploy/destroy/start/stop/status. The stack uses prebuilt images and
    docker/elastic/.env.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, stack, elastic, docker]

dependencies: []
