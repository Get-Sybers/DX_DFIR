---
galaxy_info:
  role_name: dxdfir_stack
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Lifecycle for the docker-compose analysis backends (Elastic-native and
    SOF-ELK): deploy/destroy/start/stop/status. Elastic uses prebuilt images and
    docker/elastic/.env; SOF-ELK reuses dxdfir_deploy_sofelk for its build.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, stack, elastic, sofelk, docker]

dependencies: []
