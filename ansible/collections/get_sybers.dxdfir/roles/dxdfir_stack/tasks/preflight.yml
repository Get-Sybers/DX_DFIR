---
# FIND FACTS -> check existence AND location BEFORE touching the stack.

# The engine itself comes first (tasks/docker_ensure.yml): an engine-less or
# daemon-less host is answered there by the same playbook decisions used
# below — and the deploy playbook can opt in to installing/starting it.
- name: "stack-preflight | ensure the docker engine"
  ansible.builtin.include_tasks: docker_ensure.yml

- name: "stack-preflight | stat the compose file"
  ansible.builtin.stat:
    path: "{{ dxdfir_stack_compose_dir }}/docker-compose.yml"
  register: dxdfir_stack_compose_stat

- name: "stack-preflight | assert the compose file is present"
  ansible.builtin.assert:
    that:
      - dxdfir_stack_compose_stat.stat.exists
    fail_msg: >-
      no docker-compose.yml under {{ dxdfir_stack_compose_dir }}.
    quiet: true

# FACTS BEFORE ANY REQUIREMENT: does this host hold the stack at all?
# Containers (any state) and named data volumes are read by the compose
# project label — no .env is needed for this — so a cold host is known as
# such BEFORE the env requirement below can fail anything.
- name: "stack-preflight | read the stack's containers and volumes"
  community.docker.docker_host_info:
    containers: true
    containers_all: true
    containers_filters:
      label: "com.docker.compose.project={{ dxdfir_stack_project_name }}"
    volumes: true
    volumes_filters:
      label: "com.docker.compose.project={{ dxdfir_stack_project_name }}"
  register: dxdfir_stack_host_info

- name: "stack-preflight | is any of the stack present here"
  ansible.builtin.set_fact:
    dxdfir_stack_has_containers: "{{ _containers }}"
    dxdfir_stack_has_volumes: "{{ _volumes }}"
    dxdfir_stack_present: "{{ _containers or _volumes }}"
  vars:
    _containers: "{{ dxdfir_stack_host_info.containers | default([]) | length > 0 }}"
    _volumes: "{{ dxdfir_stack_host_info.volumes | default([]) | length > 0 }}"

# THE PLAYBOOK DECIDES what absence means (dxdfir_stack_when_absent): status
# reports it and ends the host — "no stack" is that action's ANSWER, not a
# failure; start/stop/destroy flag it — they have nothing to act on; deploy
# continues — absence is its normal starting point.
- name: "stack-preflight | report the absent stack and end"
  when:
    - dxdfir_stack_when_absent == 'report'
    - not dxdfir_stack_present | bool
  block:
    - name: "stack-preflight | no analysis stack on this host"
      ansible.builtin.debug:
        msg: >-
          no analysis stack on this host: compose project
          '{{ dxdfir_stack_project_name }}' has no containers and no data
          volumes — nothing has been deployed (or it was destroyed with its
          volumes removed). Deploy with: dxdfir deploy stack.

    - name: "stack-preflight | end the play for this host"
      ansible.builtin.meta: end_host

- name: "stack-preflight | flag the absent stack"
  ansible.builtin.assert:
    that:
      - dxdfir_stack_present | bool
    fail_msg: >-
      nothing to {{ dxdfir_stack_action }}: no analysis stack on this host —
      compose project '{{ dxdfir_stack_project_name }}' has no containers and
      no data volumes. Deploy first: dxdfir deploy stack.
    quiet: true
  when: dxdfir_stack_when_absent == 'fail'

# From here every action needs the operator's .env: the compose file guards
# its secrets with ${VAR:?}, so compose cannot even parse (up, down or ps)
# without it. COLD START (issue #290): the deploy playbook opts in to
# scaffolding a default .env from .env.example (dxdfir_stack_env_scaffold —
# a playbook decision, not the role's) — and only while the host holds no
# stack state at all: fresh secrets cannot match the credentials already
# baked into existing data volumes, so a present stack demands its own .env
# back instead.
- name: "stack-preflight | stat the elastic env file"
  ansible.builtin.stat:
    path: "{{ dxdfir_stack_env_file }}"
  register: dxdfir_stack_env_stat

- name: "stack-preflight | scaffold a default .env (cold host, playbook opt-in)"
  ansible.builtin.include_tasks: env_scaffold.yml
  when:
    - dxdfir_stack_env_scaffold | bool
    - not dxdfir_stack_env_stat.stat.exists
    - not dxdfir_stack_present | bool

- name: "stack-preflight | the stack is present — assert its .env survives"
  ansible.builtin.assert:
    that:
      - dxdfir_stack_env_stat.stat.exists
    fail_msg: >-
      the stack is present on this host (containers or data volumes exist)
      but {{ dxdfir_stack_env_file }} is gone. Restore the operator .env — a
      newly generated one cannot match the credentials already baked into the
      data volumes. For a true cold start, destroy with
      dxdfir_stack_remove_volumes=true first, then redeploy.
    quiet: true
  when: dxdfir_stack_present | bool

- name: "stack-preflight | assert the elastic .env is present (cold host)"
  ansible.builtin.assert:
    that:
      - dxdfir_stack_env_stat.stat.exists
    fail_msg: >-
      {{ dxdfir_stack_env_file }} is missing — `dxdfir deploy stack` scaffolds
      a default one on a cold host (its playbook sets
      dxdfir_stack_env_scaffold), or copy docker/elastic/.env.example to
      docker/elastic/.env and set the passwords/keys yourself.
    quiet: true
  when: not dxdfir_stack_present | bool

# PULL-CAPACITY GATE (dxdfir_stack_pull_gate — enabled by the playbooks
# whose action pulls images: deploy/start). A short image store fails
# mid-pull with containerd's "no space left on device", so: images still to
# pull -> measure the stores -> reclaim once when short (a prune is a no-op
# when there is nothing to reclaim) -> re-measure -> assert. A doomed pull
# becomes one clear failure, a recoverable one self-heals, and a host whose
# images are already local is never gated. stop/destroy/status leave the
# gate off — they are how an operator frees space.
- name: "stack-preflight | pull-capacity gate"
  when: dxdfir_stack_pull_gate | bool
  block:
    - name: "stack-preflight | the images the stack runs"
      ansible.builtin.command:
        argv:
          - docker
          - compose
          - --project-directory
          - "{{ dxdfir_stack_compose_dir }}"
          - --env-file
          - "{{ dxdfir_stack_env_file }}"
          - config
          - --images
      register: dxdfir_stack_images
      changed_when: false

    - name: "stack-preflight | which of them are already local"
      community.docker.docker_image_info:
        name: "{{ dxdfir_stack_images.stdout_lines | unique }}"
      register: dxdfir_stack_local

    - name: "stack-preflight | the images still to pull"
      vars:
        # RepoTags of every stack image docker knows locally. RepoTags is null
        # on digest-only/untagged images, so guard the pipeline the same way
        # dxdfir_images' ensure_built does.
        _local_tags: >-
          {{ (dxdfir_stack_local.images | default([]))
             | map(attribute='RepoTags') | flatten | select('string') | list }}
      ansible.builtin.set_fact:
        dxdfir_stack_missing_images: >-
          {{ dxdfir_stack_images.stdout_lines | unique | difference(_local_tags) }}

    - name: "stack-preflight | gate the stores while a pull is coming"
      when: dxdfir_stack_missing_images | length > 0
      vars:
        dxdfir_stack_need_bytes: "{{ dxdfir_stack_min_free_gib | int * 1024 * 1024 * 1024 }}"
      block:
        - name: "stack-preflight | measure free space on the image stores"
          ansible.builtin.command:
            argv: ["df", "--output=avail", "--block-size=1", "{{ item }}"]
          loop: "{{ dxdfir_stack_image_store_paths }}"
          register: dxdfir_stack_df
          changed_when: false
          failed_when: false

        - name: "stack-preflight | the tightest store decides"
          ansible.builtin.set_fact:
            dxdfir_stack_free_bytes: "{{ (_avail | min) if _avail | length > 0 else -1 }}"
          vars:
            _avail: >-
              {{ dxdfir_stack_df.results | selectattr('rc', 'equalto', 0)
                 | map(attribute='stdout_lines') | map('last')
                 | map('trim') | map('int') | list }}

        - name: "stack-preflight | assert an image store was measurable"
          ansible.builtin.assert:
            that:
              - dxdfir_stack_free_bytes | int >= 0
            fail_msg: >-
              none of dxdfir_stack_image_store_paths
              ({{ dxdfir_stack_image_store_paths | join(', ') }}) exists on this
              host — point it at the docker data-root.
            quiet: true

        - name: "stack-preflight | reclaim docker space (self-heal, only when short)"
          community.docker.docker_prune:
            containers: true
            images: true
            images_filters:
              dangling: "{{ not (dxdfir_stack_reclaim_all_images | bool) }}"
            networks: true
            builder_cache: true
          register: dxdfir_stack_prune
          changed_when: >-
            (dxdfir_stack_prune.containers | default([]) | length
             + dxdfir_stack_prune.images | default([]) | length
             + dxdfir_stack_prune.networks | default([]) | length) > 0
            or (dxdfir_stack_prune.builder_cache_space_reclaimed | default(0) | int) > 0
          when: dxdfir_stack_free_bytes | int < dxdfir_stack_need_bytes | int

        - name: "stack-preflight | re-measure after the reclaim"
          ansible.builtin.command:
            argv: ["df", "--output=avail", "--block-size=1", "{{ item }}"]
          loop: "{{ dxdfir_stack_image_store_paths }}"
          register: dxdfir_stack_df
          changed_when: false
          failed_when: false
          when: dxdfir_stack_prune is not skipped

        - name: "stack-preflight | the tightest store, after the reclaim"
          ansible.builtin.set_fact:
            dxdfir_stack_free_bytes: "{{ (_avail | min) if _avail | length > 0 else -1 }}"
          vars:
            _avail: >-
              {{ dxdfir_stack_df.results | selectattr('rc', 'equalto', 0)
                 | map(attribute='stdout_lines') | map('last')
                 | map('trim') | map('int') | list }}
          when: dxdfir_stack_prune is not skipped

        - name: "stack-preflight | assert the stores can hold the pull"
          ansible.builtin.assert:
            that:
              - dxdfir_stack_free_bytes | int >= dxdfir_stack_need_bytes | int
            fail_msg: >-
              {{ dxdfir_stack_missing_images | length }} stack image(s) still to
              pull, but the tightest of {{ dxdfir_stack_image_store_paths | join(', ') }}
              has {{ ((dxdfir_stack_free_bytes | int) / 1073741824) | round(1) }} GiB free
              (dxdfir_stack_min_free_gib wants {{ dxdfir_stack_min_free_gib }}).
              The reclaim already ran — free space (docker system prune -a, drop
              stale build output/evidence, grow the disk) and rerun.
            success_msg: >-
              image stores clear the pull:
              {{ ((dxdfir_stack_free_bytes | int) / 1073741824) | round(1) }} GiB free
              >= {{ dxdfir_stack_min_free_gib }} GiB.
            quiet: true
