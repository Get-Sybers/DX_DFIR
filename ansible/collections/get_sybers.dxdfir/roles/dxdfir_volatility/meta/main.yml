---
galaxy_info:
  role_name: dxdfir_volatility
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process memory images into per-plugin JSON Lines.
    docker-runs the hardened, env-driven get-sybers/flashback image;
    one <plugin>.jsonl per image via the custom jsonl_dfir renderer.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, volatility, memory, forensics]

dependencies: []
