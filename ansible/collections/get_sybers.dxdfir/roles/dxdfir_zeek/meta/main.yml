---
galaxy_info:
  role_name: dxdfir_zeek
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process PCAPs into Zeek JSON logs. Runs the self-orchestrating
    get-sybers/zeek image from its contract; one folder of *.json per capture.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, zeek, pcap, network]

dependencies: []
