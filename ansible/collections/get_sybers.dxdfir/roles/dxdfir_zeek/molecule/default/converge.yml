---
- name: Converge
  hosts: localhost
  gather_facts: true
  vars:
    dxdfir_zeek_pcap_dir: /tmp/dxdfir_zeek_molecule/pcaps
    dxdfir_zeek_out_dir: /tmp/dxdfir_zeek_molecule/out
  tasks:
    - name: "converge | run dxdfir_zeek"
      ansible.builtin.include_role:
        name: dxdfir_zeek
